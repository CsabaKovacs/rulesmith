name: flutter_noisy_realish_min
description: Noisy Flutter fixture with host wrappers, migrations, tests, and layered app code.
publish_to: 'none'

environment:
  sdk: ^3.5.0

dependencies:
  flutter:
    sdk: flutter
  supabase_flutter: ^2.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
